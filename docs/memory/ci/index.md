---
description: "PR quality gate (`npm ci` → build → typecheck → test → lint, with no `--if-present` on any workspace fan-out) and its enforcement — a completed verdict per push (no `concurrency` block) with the `test` check required to merge via ruleset 14531661 — plus the committed-lockfile reproducible-install contract shared with release.yml and the explicit-discovery test-runner contract for the Node-20.19 floor"
---
# Ci Documentation

> **Generated by `fab memory-index`** — do not hand-edit. Descriptions come from each file's `description:` frontmatter.

| File | Description |
|------|-------------|
| [pr-quality-gate](pr-quality-gate.md) | PR CI gate (`npm ci` → build → typecheck → test → lint, .github/workflows/ci.yml): every push to an open PR gets a completed verdict (no cancellation) and the `test` check is required to merge (ruleset 14531661, pinned to the Actions app); every workspace typechecks, no `--if-present` fan-outs; code-quality rules are ESLint warnings; strict explicit-discovery test runners (Node 20.19 lacks `node --test` globs); refactors batch tested oversized functions, characterize untested ones first. |
